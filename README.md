# Parser News

Small BeautifulSoup-based parser for public news pages.

This repository is kept as a simple learning example. It does not include IDE
metadata, local caches, or machine-specific files.

## Run

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install beautifulsoup4
python txt.py
```

